clone the project from git : git clone https://github.com/ravindra1986/Survey.git

build the application: mvn clean install -DskipTests
run the applicaton : mvn spring-boot:run

once application up and running 

http://localhost:8080/api/swagger-ui.html

Access the above url in browser, it will display the all the apis.